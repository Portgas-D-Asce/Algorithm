#include <iostream>
#include <vector>
using namespace std;

// for 00001111 find first 1
/*template<typename T>
ssize_t binary_search(vector<T> nums) {
    ssize_t p = 0, r = nums.size();
    while(p < r) {
        ssize_t q = (p + r) >> 1;
        check(q) ? r = q : p = q + 1;
    }
    return p;
}*/

// for 11110000 find last 1
/*template<typename T>
ssize_t binary_search(vector<T> nums) {
    ssize_t p = -1, r = n - 1;
    while(p < r) {
        ssize_t q = (p + r + 1) >> 1;
        check(q) ? p = q : r = q - 1;
    }
    return p;
}*/


int main() {

    return 0;
}
